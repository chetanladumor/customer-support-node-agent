# Database connection (PostgreSQL on host port 5455)
DATABASE_URL="postgresql://postgres:postgrespassword@localhost:5455/swadesh_support_db?schema=public"

# Server Port
PORT=3001
CORS_ORIGIN="http://localhost:5174"

# AI Provider Configuration (Optional — deterministic engine used if keys are not provided)
GOOGLE_GENERATIVE_AI_API_KEY=""
AI_MODEL="gemini-2.0-flash"
